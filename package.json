{
  "name": "pc-bot-django",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"backend,frontend\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "bash -c 'ROOT=$PWD; cd chatbot && if [ -f \"$ROOT/.venv/bin/python\" ]; then \"$ROOT/.venv/bin/python\" manage.py runserver 0.0.0.0:8000; else python3 manage.py runserver 0.0.0.0:8000; fi'",
    "dev:frontend": "npm --prefix frontend run dev",
    "start": "npm run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
