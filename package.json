{
  "name": "pc-bot-django",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix frontend run dev",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  }
}
